all_includes += include_directories('.')

Arrow_lib = static_library(
  'Arrow',
  'Arrow.cpp',
  install : false
)

Box_lib = static_library(
  'Box',
  'Box.cpp',
  install : false
)
Cross_lib = static_library(
  'Cross',
  'Cross.cpp',
  install : false
)