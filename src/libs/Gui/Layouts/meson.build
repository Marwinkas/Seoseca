all_includes += include_directories('.')

FormLayout_lib = static_library(
  'FormLayout',
  'FormLayout.cpp',
  install : false
)

Layout_lib = static_library(
  'Layout',
  'Layout.cpp',
  install : false
)
